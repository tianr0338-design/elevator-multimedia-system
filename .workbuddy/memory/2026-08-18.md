# 2026-08-18

## Git 初始化
- 在本项目根目录执行 `git init`，默认分支由 `master` 重命名为 `main`。
- 已设 `core.autocrlf=false`，避免 Windows/Linux 换行符混入库。
- 新建根目录 `.gitignore`：忽略系统文件、IDE 临时文件（.vscode/.settings/.project/.cproject/.clang-format）、编译产物（*.o/*.a/*.so/*.exe 等）、`lvgl8.3/build/`、`lvgl8.3/demo`、`lvgl8.3/project/bin/`。
- 验证结果：454 个文件被忽略（主要是 lvgl8.3 的 .o 与 build 目录），`git add .` 将纳入约 1432 个非忽略文件。
- 待定决策：lvgl8.3/lvgl/ 与 lvgl8.3/lv_drivers/ 作为 vendored 库整体纳入追踪（自包含、可离线编译）。若日后想精简仓库，可考虑 git submodule 或将其加入 .gitignore。
- 尚未做首次 commit（用户仅要求初始化 + .gitignore）。

## 编写 README.md
- 根据真实配置（lv_conf.h / main.c / Makefile / .cproject / server.c）撰写根目录 README.md。
- 关键事实：目标平台 ARM Linux 开发板，`arm-linux-gcc` 交叉编译，输出 `demo`；显示 fbdev 800×480，输入 evdev；FreeType 2.13.3 依赖（路径写死）；LVGL 32bit 色/8MB 内存/30ms 刷新/纯软件渲染/启用 SIMSUN_16_CJK 中文。
- 架构：lift_server（epoll 服务器，端口 8888，cJSON 协议，ads/group_1、group_2 分组素材）↔ 设备端 LVGL 客户端（lift_client 被 Makefile 实际编译；project/ 为备选界面未纳入默认 Makefile）。
- 已标注待确认点：活动界面是 lift_client 还是 project；FreeType 路径写死；"A:" 文件系统挂载点。
